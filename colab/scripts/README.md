# Scripts

Put Colab-friendly Python scripts here (e.g. `train.py`, `eval.py`, `export.py`).

Recommended conventions:
- Read inputs from `../data/` (ignored)
- Write outputs to `../artifacts/` (ignored)

