[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "laprop"
version = "0.0.0"
description = "Laptop recommender (refactored)"
requires-python = ">=3.9"
